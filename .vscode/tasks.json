{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Git Commit All and Push",
      "type": "shell",
      "command": "git add . && (git diff --staged --quiet || git commit -m 'Auto-save commit') && git push",
      "group": "build",
      "presentation": {
        "echo": true,
        "reveal": "silent",
        "focus": false,
        "panel": "shared"
      }
    }
  ]
}
